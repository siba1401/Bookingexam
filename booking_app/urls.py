from django.urls import path
from . import views

urlpatterns = [
    # Example: If you want a specific non-admin dashboard
    # path('dashboard/', views.booking_dashboard, name='booking_dashboard'),
]